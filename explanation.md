Among the three regression models tested, Linear Regression provided the best performance, achieving the lowest Mean Squared Error (2900.19) and the highest R² score (0.4526). This indicates that the model was able to explain approximately 45% of the variance in the dataset. Random Forest also performed reasonably well, with an R² score of 0.4094, but did not outperform Linear Regression, possibly due to lack of hyperparameter tuning. The Decision Tree model performed poorly, with a negative R² score (-0.0212), indicating that it failed to generalize and likely overfit the training data. Overall, Linear Regression was the most effective model for this dataset, likely because the relationships in the data are relatively linear.